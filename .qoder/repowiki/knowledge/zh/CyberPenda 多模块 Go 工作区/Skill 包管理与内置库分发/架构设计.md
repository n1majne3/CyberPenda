模块以 `internal/skill` 为 Go 实现层，`skills/bundles` 为运行时工作目录，`internal/skill/builtins/assets` 通过 `//go:embed` 编译进二进制作为内置资源。

- 数据模型：`skill.go` 定义 `Metadata` / `SourceProvenance` / `Skill` / `Bundle` 等核心结构，所有错误通过 `ErrInvalidSkill`、`ErrNotFound`、`ErrEnabled` 等哨兵错误表达。
- 服务层：`service.go` 暴露 `Service`（依赖 `*store.DB`、可选 `Importer`），封装 Publish/Import/List/Get/Delete/Files/EnabledSkills/SetOptOut 等事务性操作；Publish 采用 `.staging/<id>-<random>` → 校验 → rename 原子替换 + `.backup-*` 回滚模式，保证并发安全。
- 存储契约：元数据写入 `skills` 表（`source_provenance_json` 存 JSON），启用状态由 `skill_profile_opt_outs` 反向标记（opt-out 而非白名单），`EnabledSkills` 用 NOT EXISTS 子查询过滤。
- 文件布局：每个 Skill 对应 `libraryRoot/bundles/<id>/` 目录，根必须包含 `SKILL.md`；`Files(id)` 递归读取并拒绝 symlink 与路径穿越。
- 导入扩展点：`importer.go` 定义 `Importer` 接口，生产实现 `NPXImporter` 固定调用 `npx --yes skills import --package <pkg> [--ref] [--source-url] --json`，禁止任意 shell 命令注入。
- 内置技能：`builtin.go` 扫描 embed FS 中 `builtins/assets/*` 目录，解析 SKILL.md front-matter 生成 `ImportedBundle`，并通过 `InstallBuiltinSkills` 在启动时幂等安装；同时维护 `prunedBuiltinSuccessors` 与 `retiredPrunedBuiltinIDs` 两张映射，完成旧 ID 迁移与退役清理。
- 展示层：`display.go` 提供 `DisplayID`/`DisplayName`，对 `cyberstrikeai-`/`strix-` 前缀做去源前缀处理，仅用于 UI 展示，不影响存储 ID。
- 校验层：`validation.go` 集中校验 ID 正则、bundle 根存在性与非 symlink、相对路径穿越防护，被 Publish/Import/Builtin 三处复用。

依赖方向：`service` → `importer`（接口）、`store.DB`（SQL）；`builtin` 仅依赖 `service` 与标准库；`display` 无外部依赖。