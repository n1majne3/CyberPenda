- 对外错误统一使用包级 `var ErrXxx = errors.New(...)` 常量，并通过 `errors.Is` 在调用方判断，而非返回字符串消息。
- 输入规范化采用独立的 `Normalize*` 函数族（`NormalizeBaseURL`/`NormalizeProtocols`/`NormalizeEndpoints`/`normalizeRequestedEndpoints`），在 Create/Update/Backfill 等多处复用，避免重复校验逻辑。
- JSON 列读写走统一的 `encode`/`scanProvider` 辅助，所有 SQL 查询只取固定列并按序 Scan，再反序列化为结构体字段。
- 可选更新参数一律使用指针类型（如 `UpdateRequest` 中 `*string`/`*[]Protocol`），通过 nil 判断区分“未修改”和“显式清空”。
- 枚举值集中声明于包级 `const` 块，并用同名的白名单 map（如 `validProtocols`）做合法性校验。