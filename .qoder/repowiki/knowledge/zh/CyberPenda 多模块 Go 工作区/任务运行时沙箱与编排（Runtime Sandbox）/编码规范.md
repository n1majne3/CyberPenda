- 所有 Provider 运行时必须实现 `runtime.Adapter` 接口，禁止绕过 Harness 直接 exec 子进程
- Runner 仅产出无副作用的 `Command`，实际进程生命周期由 `runtime.Harness` 统一管理
- 敏感值一律通过 `adapters.NewRedactor(EnvSecretValues(env))` 在写入 task 事件前脱敏，不得在日志/事件中透传明文
- 沙箱内 Provider Home 固定挂载于 `/task/runtime-home/<provider>`，并通过 `providerHomeEnv()` 约定的环境变量名注入