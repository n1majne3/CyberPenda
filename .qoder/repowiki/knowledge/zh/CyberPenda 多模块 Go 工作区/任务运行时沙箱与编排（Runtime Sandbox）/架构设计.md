本模块由三层契约串联：
- 顶层 `internal/runtime` 定义 `Adapter` 接口与 `Harness` 生命周期管理，所有 Provider 具体实现必须通过该接口暴露 `Run(ctx, goal, emit)`；`Harness` 负责注册 active run、向 task 服务追加事件、处理 Stop/Finish 竞态以及可选的 `metadataRecordingAdapter` 回调。
- 中间层 `internal/runtimeprofile` 提供全局可编辑的 Profile 模型与 SQLite 持久化，被 runner 在启动期按 Provider/ModelProviderID 自动匹配，再由 runner 投影为 Provider 本地配置文件与环境变量。
- 入口层 `internal/runner` 不直接运行进程，而是根据 Profile + Blackboard v2 布局生成无副作用的 `Command`（host 或 sandbox），并通过 `BuildSandboxCommand` 把 `Layout.ProviderHome` 映射进容器 `/task/runtime-home/<provider>`，同时注入 `PI_CODING_AGENT_DIR`/`CLAUDE_HOME`/`CODEX_HOME` 等环境变量。
- 运行时选择由 `ValidateActivation` 与 `SelectAfterSandboxFailure` 统一决策：sandbox 失败不允许回退 host，host 模式需显式激活；`HostProxyOnlySandboxNetworkName` 与 `pentest-host-proxy-only` entrypoint 共同约束容器网络出口。
- 输出侧 `output_reader` + `ScanOutputWithObserver` 对 stdout/stderr 行扫描，结合 `adapters.Redactor(EnvSecretValues(...))` 在写入 task 事件前脱敏，并由 `NativeSessionMetadataFromRuntimeLine` 解析容器 ID/原生会话路径回写至 continuation metadata。