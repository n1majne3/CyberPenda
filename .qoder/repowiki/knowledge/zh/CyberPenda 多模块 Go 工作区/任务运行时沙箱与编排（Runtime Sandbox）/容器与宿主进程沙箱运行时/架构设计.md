本模块围绕 `Adapter` 接口（由上层 `internal/adapters` 定义）提供两类可插拔沙箱后端：
- `docker_sandbox.go`：基于 `docker create/start/rm` 子进程的容器适配器，通过 `DockerSandboxConfig` 注入镜像、网络要求与日志回调；在 `Run` 中依次完成网络存在性校验/创建、镜像拉取、`create` + `start -a` 生命周期管理，并通过 goroutine 监听 stdout/stderr，使用 `ScanOutputWithObserver` 解析 `NativeSessionMetadata` 行。
- `host_process_unix.go` / `host_process_windows.go`：按构建标签编译的本地进程启动器，Unix 侧通过 `Setpgid` 建立进程组并使用 `SIGTERM→SIGKILL` 两阶段终止，Windows 侧仅能终止根进程（注释说明 Job Objects 未实现）。
- `container.go`：提供 `StopConfirmation` 钩子与 `ConfirmDockerContainerExited`，通过轮询 `docker inspect` 确认容器退出，供上层在 adapter 返回后做资源收尾。
- `codex_session.go`：以 best-effort 方式从 provider home 下 `.jsonl` 或 `state*.sqlite` 文件逆向发现 Codex 会话 ID，作为 `NativeSessionMetadata` 的来源之一。
- `done_signal.go`：通用 `FirstSignal` 原语，将多个关闭信号合并为单通道，被各桥接器用于统一结束 Task 作用域。
依赖方向：本模块仅向上暴露 `Adapter` 实例与若干工具函数，向下依赖 `os/exec`、`syscall`、`adapters`（红脱敏）、`task`（事件类型），不反向依赖上层 daemon 或 blackboard。