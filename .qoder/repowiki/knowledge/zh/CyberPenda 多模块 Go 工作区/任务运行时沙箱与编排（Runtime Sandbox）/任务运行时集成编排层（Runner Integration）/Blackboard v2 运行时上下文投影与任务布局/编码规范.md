- 对每个受支持的 Provider 都暴露一组别名函数（如 `PrepareCodexV2TaskLayout` → `PrepareBlackboardV2TaskLayout`、`ProjectCodexV2RuntimeConfig` → `ProjectBlackboardV2RuntimeConfig`、`ErrUnsafeCodexV2Layout = ErrUnsafeBlackboardV2Layout`），以便遗留调用方无需修改。
- 所有对外部路径参数执行白名单校验后再进入文件系统操作：`safeBlackboardV2PathComponent` 拒绝 `..`、绝对路径与包含分隔符的值；`validateExistingBlackboardV2Directory` / `openBlackboardV2Directory` 逐段 Lstat 并比对 SameFile 防止竞态。
- Provider 差异化通过 switch 在入口分发（`PrepareBlackboardV2TaskLayout`、`ProjectBlackboardV2RuntimeConfig`、`blackboardV2InstructionFile`、`blackboardV2ObsoleteInstructionFiles`），新增 Provider 只需追加分支而不改动共享逻辑。
- 敏感信息一律从模型可见上下文剥离：`BlackboardV2ProcessEnv` 显式丢弃 PENTEST_* 身份与接口凭据；`projectXxxV2RuntimeConfig` 在构造内部 projectionRequest 前清空 ProjectID/TaskID/DaemonAddr/AuthToken/Credentials。
- 文件写入采用临时文件 + rename 原子替换模式（`writeRootFileAtomically` 与测试中 `replaceTestFileAtomically`），确保只读 bind mount 下的容器能观察到新 inode 内容。