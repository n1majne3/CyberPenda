- 对外构造型（Layout/Command/SandboxCommandRequest）保持纯数据、无副作用，实际执行由上层 harness 负责
- Provider 相关常量（home 目录名、环境变量名、协议映射）集中在 `providerHomeDir`/`providerHomeEnv` 单点维护，禁止散落在各 projectXxx 函数中
- 含 API Key/Token 的配置一律通过 `writeJSONConfigFile` 以 0o600 权限落盘，预览字段使用 `redact*` 脱敏后再返回