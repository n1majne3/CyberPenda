本模块位于 `internal/runner`，是 runner_integration 层中面向“命令构建”的边界：
- 入口契约由 `task.Runner`（通过 `Runner = task.Runner` 别名）和 `task.RunnerSandbox` / `task.RunnerHost` 常量定义，本模块仅消费这些类型，不持有进程句柄。
- `Layout` + `PrepareTaskLayout` 负责在 `task_root` 下创建 `workdir/runtime-home/<provider>/skills/artifacts/logs` 目录树；`ConfigProjection` 描述写入 provider home 的配置快照。
- `BuildSandboxCommand` 以 `SandboxCommandRequest` 入参构造无副作用的 `Command{Program, Args}`，默认使用 `kalilinux/kali-rolling` 镜像并通过 `--mount type=bind` 将 `task_root` 只读挂载到 `/task`，工作目录固定为 `/task/workdir`。
- 网络隔离通过 `SandboxNetworkMode` 控制：`host_proxy_only` 模式启用自定义桥网络 `pentest-host-proxy-only` 并注入 entrypoint `/usr/local/bin/pentest-host-proxy-only`，同时要求 `NET_ADMIN` 能力。
- `target.go` 提供 `RewriteLoopbackTargets`，在 sandbox=true 时将 `127.0.0.1` / `localhost` 重写为 `host.docker.internal`，支持 URL 与裸 host token 两种场景。
- `pi_sandbox.go` 针对 Pi 运行时在无预装二进制时通过 npm 安装 `@earendil-works/pi-coding-agent` 并 `exec pi`，包名与版本由 `PENTEST_PI_NPM_PACKAGE` / `PENTEST_PI_NPM_VERSION` 环境变量覆盖。
- 依赖方向单向向外：依赖 `internal/task`、`internal/runtimeprofile`、`internal/modelprovider`，不被同层其他文件反向引用，测试文件集中在 `*_test.go` 中以白盒方式验证路径穿越防护、URL 重写与 Pi 包装逻辑。