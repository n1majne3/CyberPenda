- 每个领域包暴露一个 `Service` 结构体加 `NewService(db *store.DB, ...)` 构造器，方法返回 `(DomainType, error)` 并以包级 `var ErrNotFound = errors.New(...)` 表达未找到语义。
- 复杂可变字段在数据库中以 `<field>_json` 列存储，写入前 `json.Marshal`，读取后由包内 `scanXxx(row scanner)` 辅助函数统一 `json.Unmarshal` 并包装错误。
- 需要顺序保证的操作（追加 Event、RecordRuntimeConfig、CreateContinuationLaunchTx）使用 `db.Begin()` 事务，先 `SELECT MAX(seq|version|number)` 再插入，确保并发安全。
- 对外暴露的错误采用包级 `errors.New` 常量（如 `ErrNotFound`、`ErrMissingName`、`ErrActiveTask`），上层通过 `errors.Is` 判断而非字符串匹配。
- 时间戳一律 `time.Now().UTC()` 生成并以 `time.RFC3339Nano` 格式化存入，扫描时再用相同格式解析，避免本地时区污染。
- ID 生成统一走包内 `newID()` 使用 `crypto/rand` 产生 16 字节随机数，失败回退到 RFC3339Nano 字符串。