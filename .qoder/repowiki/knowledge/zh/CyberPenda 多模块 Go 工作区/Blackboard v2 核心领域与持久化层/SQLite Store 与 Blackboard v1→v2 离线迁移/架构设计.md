模块由两个子包组成，职责边界清晰：
- `internal/store`：所有领域共享的 SQLite 层。通过 `Open(path)` 打开数据库并执行 `migrations()` 中按版本号排序的 DDL；空路径走内存库（测试用）。`blackboard_store_state.canonical_store` 字段标识 epoch（`legacy_v1` / `graph_v1` / `graph_v1_finalized` / `blackboard_v2`），`Open` 在运行时拒绝 v1 epoch，仅允许 v2。
- 离线只读入口 `OpenMigrationSource` 使用 `openReadOnlyInspection` + `immutable=1` 模式，并在 WAL 存在时把主文件+sidecar 复制到临时目录再读取，保证对源库零写；随后 `classifyMigrationSource` 区分 `numbered_v1`（有 `schema_migrations`）和 `pre_numbered_v1`（靠 `validatePreNumberedLegacySchema` 做列指纹比对）两类来源。
- 可写离线入口 `OpenWritableMigrationDB` 先只读分类，再以可写方式打开并应用 21-32 号“增量”迁移但不翻转 epoch，供 rebuild/cutover 在同一事务内完成。
- `CompleteBlackboardV2Cutover` 是 v1→v2 破坏性收尾点，由调用方在事务内校验通过后显式调用。
- `internal/blackboardmigration`：面向 CLI/daemon 的离线迁移编排器。`Service.Execute` 根据 `MigrationKind`（inspect/backup/verify/rebuild_unambiguous/migrate）分派；`migrateToBlackboardV2` 在一个 SQLite 事务内串联 source_digest 校验 → 备份验证 → 活跃 Continuation 阻塞检查 → `rebuildUnambiguousHeadsWithTx` → 应用 staged Scope limits → 调用 `blackboardv2.NewServiceWithEvidence` 做 parity 校验 → `store.CompleteBlackboardV2Cutover` → 翻转 `canonical_store` 为 `blackboard_v2` → 记录 migration 20 → 提交。
- `rebuild_v2.go` 将 v1 graph/legacy 表重放为 disposable `blackboard_v2_*` 表，支持用户决策（Mapping/Scope Limits）并通过 `blackboardv2contract` 校验快照一致性。
- `backup.go` 提供 `SQLiteBackupImplementation`（WAL checkpoint + `VACUUM INTO` + 独立只读 quick_check + SHA256）与针对只读源的逻辑备份实现，均返回 `VerifiedBackup`。
- 依赖方向单向：`blackboardmigration` 依赖 `store` 与 `blackboardv2`，`store` 不反向依赖迁移包。