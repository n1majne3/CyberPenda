模块由两个文件组成：
- `credential.go` 定义核心领域模型（`Source`、`Binding`、`Resolution`、`Scope`）与 `Service` 业务层，通过 SQLite 的 `credential_bindings` 表持久化，按 `(credential_ref, scope, scope_id)` 唯一约束实现“每个引用在每个作用域仅一个绑定”；`Resolve` 严格遵循“项目覆盖 → 全局回退 → 未找到”的解析顺序，且被显式 `disabled` 的项目绑定会阻断全局回退。
- `materialize.go` 负责把 `Source` 实际读取为字符串值（`Materialize`），并组合成运行时 env 映射（`ResolveMaterializedEnv` / `ResolveSourceEnv` / `destinationEnv`），其中 `command` 源默认禁用，需设置 `PENTEST_ALLOW_COMMAND_CREDENTIALS` 才允许执行任意 shell。
依赖方向单向：本模块依赖 `pentest/internal/store.DB` 访问数据库，不反向依赖上层 API 或 runner；对外暴露的最小契约是 `Service` 方法与包级 `Sanitize*` 函数，用于向 API 客户端安全返回（用 `[configured]` 哨兵替换 literal 值）。