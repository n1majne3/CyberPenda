- 所有写操作统一通过 store.Open 获取 DB，禁止各子包自行 sql.Open，以保证全局唯一的 MaxOpenConns=1 与迁移编排。
- 对现有 v1 数据库一律使用 OpenMigrationSource 只读打开，禁止直接 Open 旧库，错误信息必须引导用户运行 'blackboard v2 inspect/migrate/verify'。
- 迁移定义集中在 migrations() 列表，每个 migration 包含 version/name/checksum/up 四元组，新增迁移不得修改已应用版本的 checksum。
- 领域 Service 方法签名以 context.Context 开头，错误通过 errors.Is 可组合返回，测试使用 :memory: DSN 隔离并发。
- 事件载荷输出一律经 adapters.Redactor 脱敏，禁止将解析后的凭据值拼入命令行或日志。